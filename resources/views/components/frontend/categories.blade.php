<div class="text-center group transition duration-300 transform hover:scale-105 hover:shadow-shadowPrimary rounded-xl overflow-hidden">
    <img class="w-auto rounded-xl object-cover mx-auto transition duration-300 transform group-hover:scale-105 group-hover:shadow-shadowPrimary" src="{{ $categories['image'] }}" alt="{{ __('image') }}">
    <p class="py-2 transition duration-300 group-hover:text-text.secondary">{{ __($categories['name']) }}</p>
</div>

