
<div>

    <div class="text-center">
        <img class="w-auto rounded-xl object-cover mx-auto" src="{{ $categories['image'] }}" alt="{{ __('image') }}">
        <p class="py-2">{{ __($categories['name']) }} </p>
    </div>


</div>
